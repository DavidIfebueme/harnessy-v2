/** Current CLI and lockfile-facing package version. */
export const HARNESSY_VERSION = "0.0.3";

/** Default project-local directory for Harnessy state. */
export const DEFAULT_HARNESS_DIR = ".harnessy";

/** Lockfile basename written inside the Harnessy directory. */
export const LOCKFILE_NAME = "harnessy.lock.json";

/** Directory name for project-local agent context. */
export const CONTEXT_DIR_NAME = "context";

/** Directory name for runtime profile templates. */
export const PROFILES_DIR_NAME = "profiles";

/** Directory name for installed capability manifests and artifacts. */
export const CAPABILITIES_DIR_NAME = "capabilities";

/** Directory name for scoped memory templates. */
export const MEMORY_DIR_NAME = "memory";
