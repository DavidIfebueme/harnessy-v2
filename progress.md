# Progress

## 2026-07-11

- Located and verified the canonical Python Jarvis implementation.
- Confirmed the preserved Harnessy v2 snapshot matches the canonical source.
- Generated the actual Click command tree and identified stale machine-readable docs.
- Inventoried source/test size, domain modules, protocols, configuration, state paths, environment variables, and current Effect overlap.
- Created `task_plan.md` for Plannotator review with migration phases, decision points, exit gates, and a proposed first slice.
- Created `findings.md` with the audit evidence.
- Incorporated Plannotator review decisions for state authority, `hsy` ownership, agent surface, channel daemons, compatibility lifecycle, and rollback.
- Researched the initial dual-transport Notion proposal and replaced it with one private, version-pinned Effect HTTP connector for current CLI, embedded, and headless requirements.
- Deferred hosted MCP and OAuth/PKCE until delegated interactive act-as-user access is demonstrated; retained semantic normalization, redacted credentials, and REST fixture-drift requirements.
- Deferred write approvals, scoped grants, idempotency policy, and mutation auditing to a GitHub issue that must be opened as the first execution task.

## Implementation start

- Plannotator approved the revised plan.
- Opened deferred authorization/audit issue [#48](https://github.com/Flow-Research/harnessy-v2/issues/48) with `enhancement` and `pkg:coding-agent` labels.
- Added a deterministic Python Click-tree exporter and a frozen 131-node command manifest.
- Added a state-format inventory plus representative pending, selected-space, malformed-state, and sync fixtures.
- Added Effect `JarvisPathResolver`, `JarvisConfigReader`, `JarvisContextLoader`, `JarvisDiagnostic`, and parity manifest schemas.
- Added `harnessy jarvis diagnose [--json] [--target]`; it is read-only and redacts configuration values.
- Added the full 12-file legacy context precedence and `{{global}}` expansion behavior.
- Added a direct pinned `yaml@2.9.0` dependency.
- Added seven focused compatibility tests.
- Replaced manual legacy scalar normalization with Effect Schema codecs and schema transformations, including Pydantic-compatible integer and boolean coercion boundaries.
- Created reviewed migration issues #49–#61 and linked them from umbrella issue #35.
- Committed the verified first compatibility slice as `b4b7f07f`.
- Added a deterministic 177-entry parity ledger spanning all 131 frozen command nodes, 20 state stores, 12 context documents, connectors, workflows, channels, and extracted host capabilities.
- Added typed parity validation, per-status/per-surface summaries, and read-only `harnessy jarvis parity [--json]` reporting.

## Verification

- Focused Vitest: 1 file, 7 tests passed.
- `npm run check`: passed with zero lint warnings/errors.
- Legacy integer/boolean boundary probes were compared against the preserved Pydantic models.
- Frozen command manifest regenerated byte-identically with matching SHA-256.
- Frozen parity manifest regenerated byte-identically with SHA-256 `86ca9ddac62e27b2234e447e1f267366453e02b4391b056b177d2f7e7b68b511`.
- Added deterministic AnyType/Notion capability matrices, the complete nine-class backend error hierarchy, retry delay vectors, and executable retry-state fixtures.
- Expanded the persisted-state inventory from 14 to 20 stores, corrected exact cache/channel/wiki paths and malformed behavior, split reading/channel/thread stores, and added representative YAML/JSON/Markdown/text fixtures.
- Added a Python-model-validated state-fixture oracle whose hashes are enforced by native compatibility tests.
- Fixed legacy `jarvis docs --json` by attaching the complete live 131-node Click tree while preserving the existing curated examples and human Markdown output.
- Added Python regression tests proving machine-readable docs match every registered Click node.
- Added native `Schema.Class` configuration models with Pydantic-compatible defaults and typed YAML/environment merging.
- Added injected `JarvisEnvironment` support for `JARVIS_*` and nested `__` overrides, including named account deep merges.
- Added redacted credential resolution for backend, Fathom, and WhatsApp secrets with environment/managed-file precedence and tagged missing/unknown-account errors.
- Matched case-insensitive Pydantic environment JSON/nested merge semantics and shell-compatible first-export parsing for managed Fathom secrets.
- Added read-only credential-presence diagnostics exposing only source and variable-name metadata; tests verify no secret values or canonical-store writes.
- Added a bounded, symlink-rejecting `JarvisStateReader` covering all 20 versioned state-manifest stores with sanitized readiness issue codes.
- Added per-store absent/readable/partial/invalid diagnostics and safe-to-import/needs-review/blocked aggregation without repair or migration writes.
- Added fixture, malformed/mixed, missing, symlink, size-bound, deterministic, and filesystem-snapshot tests for state inspection.
- State reads use descriptor-relative `O_NOFOLLOW` component walking on POSIX; Windows fails closed with blocked `STATE_IO_UNAVAILABLE` until a safe relative-handle primitive is available.
- Replaced the native monolithic AnyType connector surface with focused Effect knowledge services for capabilities, spaces, tasks, journal, tags, objects, collections, and files.
- Added shared `Schema.Class` semantic models, explicit capability evidence, and typed transport/auth/rate-limit/not-found/data/unsupported errors.
- Normalized AnyType list-spaces, object search, and object get behind the shared contracts; unsupported reads return evidence instead of empty results.
- Added policy, idempotency, and audit metadata to mutation request schemas while every mutation deterministically fails with `ConnectorMutationDisabledError` for #48 before transport access.
- Added cancellation and typed failure coverage; focused AnyType contract tests pass 11/11 and `npm run check` passes.
- Researched hosted Notion MCP versus direct HTTP and selected one private Effect HTTP transport; MCP/OAuth are deferred until delegated interactive access is a demonstrated requirement.
- Added a redacted, version-pinned Notion read connector for configured workspace, tasks, journal, tags, objects, and search with explicit schemas, bounded cursor pagination, typed retry/error handling, and cancellation.
- Added legacy-compatible task/journal/block/property normalization, explicit unsupported evidence, and transport-free #48 mutation failures.
- Added deterministic in-memory Notion tests covering normalization, capabilities, pagination cycles, status classification, retries, secrecy, mutation blocking, and interruption.
- Focused compatibility suite after the oracle expansion: 1 file, 9 tests passed.
- Live source CLI smoke: `jarvis diagnose empty missing 12` under isolated HOME/target, with no files created.
- Frozen command manifest regenerated byte-identically.

## Errors encountered

| Error | Resolution |
| --- | --- |
| Plannotator initially rejected `task_plan.md` because the Pi cwd was the parent directory | Submitted `harnessy-v2/task_plan.md` relative to the active cwd |
| GitHub label API returned 404 under the default `gh` account | Retried with the authenticated `Konan69` token; issue #48 created |
| Read-only subagents were marked failed because their structured acceptance reports were not recognized | Read and validated both completed output artifacts manually; no repository writes occurred |
| First full check rejected a generator with no `yield` | Replaced it with an `Effect.fn` returning `Effect.succeed`; full check passed |
| Reviewer found incomplete Pydantic integer/boolean coercion parity | Replaced ad hoc normalization with Effect Schema codecs and added oracle-derived boundary tests |
| Concurrent `@harnessy/engine` artifact validation hit a `partyserver` / `@cloudflare/workers-types` peer conflict | Package owner is correcting its pinned dependency before the shared repository check |
| Concurrent Harnessy preset review found unsupported Slack Swagger 2.0 and discarded API-key preset auth | Package owner must resolve both blockers before the shared repository check and dev merge |

## Current state

The approved first implementation slice and parity/oracle expansion are committed. Phase 0 is complete; Phase 1 full config/environment and versioned state readers remain.

## Next action

Validate and close #53, then add backend-neutral AnyType/Notion HTTP contract and REST drift fixtures in #54.

## 2026-07-16 — Executor-native agent integration

- Corrected the product boundary: agents install and use Executor through standard MCP; Harnessy-specific intent tools are not the protocol.
- Added exact `executor@1.5.33` dependency resolution while using the vendored source CLI during repository development.
- Changed multi-agent installation from fixed HTTP/token registration to Executor's standard stdio MCP entrypoint.
- Replaced the repository-only Vite cockpit launcher with Executor daemon startup/attach and cockpit opening.
- Rewired `hsy`'s always-present Harnessy builtin from fixed HTTP/token access to the same stdio MCP entrypoint; Executor owns daemon startup, runtimes, and state.
- Updated fresh-user/runtime help to remove the manual-engine prerequisite.
- Focused core tests passed 8/8 and the coding-agent builtin tests passed 5/5.
- Rebuilt the linked development packages and ran the actual global `hsy` TUI in tmux.
- In natural language, `hsy` discovered `executor.mcp.addServer`, registered `@modelcontextprotocol/server-everything`, requested in-terminal approvals, created a no-auth connection, discovered `get_sum`, and returned `21 + 21 = 42`.
- Closed the interactive TUI and confirmed the Executor-owned daemon remained running in the background.
- Re-read the complete changed builtin, launcher, `hsy` environment, MCP installer, cockpit registration, and focused regression-test files before final verification.
- Re-verified every original review site: root `engines`, fresh `hsy` entrypoint, cockpit auth command, AnyType health classification, Windows `npx.cmd`, npx-level `--yes`, fresh AnyType registration with pinned version, Harnessy-only gating, and stateful retry protection.
- Removed the temporary synthetic MCP harness after replacing it with the real `hsy` run.
- `git diff --check` passed and the index is empty; `.pi/todos/` remains untracked and out of scope.
- Replaced the stale root `hsy:web` Vite shortcut with canonical `harnessy web` and updated the README so first use is conversational, cockpit is optional, state is Executor-owned, and external agents install stdio without token copying.
- Repository-wide stale-path search now finds no old `engine-dev`, fixed HTTP engine, `mcp serve`, or mandatory-`harnessy web` guidance outside the intentional removal assertion in a test.
- Verified `@harnessy/core` resolves the exact direct dependency `executor@1.5.33`, with matching package-lock entries.
- Confirmed the requested target is local `dev` at `cdcd253d` / `origin/dev`; the working branch is `feat/vendor-executor` with uncommitted implementation and `.pi/todos/` excluded.
- Deferred the single full repository check until the concurrent `@harnessy/engine` and preset jobs finish their shared package/lockfile fixes, avoiding duplicate high-memory validation.
- Concurrent engine validation now builds its bundled ESM/declarations successfully; its packed Cloudflare consumer fixture is being corrected for Worker type-version and declaration compatibility before the shared check.
- Concurrent preset work replaced the unsupported Slack Swagger 2.0 source with an OpenAPI 3.0 source and re-passed package typecheck, lint, and 9 tests; its auth-flow reviewer is still closing the remaining blocker.
- Full `npm run check` passed across 903 files, pinned dependencies, TS import/workspace checks, shrinkwrap/install lock, root typecheck, Harnessy lint, and browser smoke. Biome formatted four files during the successful run.
